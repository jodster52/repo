info:
  name: GetPetByStatusAvailable
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/v2/pet/findByStatus?status=available"
  params:
    - name: status
      value: available
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
