info:
  name: GetPetByStatusSold
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/v2/pet/findByStatus?status=sold"
  params:
    - name: status
      value: sold
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
